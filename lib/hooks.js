/**
 * Defines hooks required by restify-oauth2 to handle password-based
 * authentication.
 */

var hook = {};

exports = module.exports = hook;


hook.validateClient = function (credentials, req, cb) {
};


hook.grantUserToken = function (credentials, req, cb) {
};


hook.authenticateToken = function (token, req, cb) {
};
